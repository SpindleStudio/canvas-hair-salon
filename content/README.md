# content

Markdown/MDX content where a CMS isn't warranted (CLAUDE.md Section 4). Empty
on the base template - nothing here needs MDX until the blog add-on
(`siteConfig.hasBlog`, see `lib/config.ts`) is turned on for a client, at
which point post content lives here.
